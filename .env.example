# Gemini TTS (Bangla engine = Gemini). Keep this server-side only — never expose in the browser.
# Copy to `.env.local` and restart `npm run dev` after changing it.
# Used by the Vite /api/tts middleware (dev/preview) and the Vercel api/tts function (production).
GEMINI_API_KEY=
